# Direnv config for this repo.
# Kept minimal: delegate to your existing CUDA env script.

watch_file "$HOME/build_cuda.zsh"

if [[ -f "$HOME/build_cuda.zsh" ]]; then
  source "$HOME/build_cuda.zsh"
else
  echo "warning: $HOME/build_cuda.zsh not found; CUDA env not configured" >&2
fi
